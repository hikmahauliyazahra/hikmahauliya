/* =========================
   RESET
========================= */

* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

html {
    scroll-behavior: smooth;
}

body {
    font-family: Arial, Helvetica, sans-serif;
    background-color: #f8fbff;
    color: #26364a;
    line-height: 1.6;
}


/* =========================
   NAVBAR
========================= */

header {
    background-color: rgba(255, 255, 255, 0.95);
    position: sticky;
    top: 0;
    z-index: 1000;
    box-shadow: 0 2px 15px rgba(80, 130, 180, 0.08);
}

.navbar {
    max-width: 1100px;
    margin: auto;
    padding: 18px 25px;

    display: flex;
    justify-content: space-between;
    align-items: center;
}

.logo {
    color: #5b9bd5;
    font-size: 25px;
}

.nav-links {
    display: flex;
    list-style: none;
    gap: 25px;
}

.nav-links a {
    text-decoration: none;
    color: #526477;
    font-size: 14px;
    transition: 0.3s;
}

.nav-links a:hover {
    color: #5b9bd5;
}


/* =========================
   HERO
========================= */

.hero {
    max-width: 1100px;
    min-height: 90vh;
    margin: auto;
    padding: 80px 25px;

    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 50px;
}

.hero-text {
    max-width: 600px;
}

.hello {
    color: #6ea8dc;
    font-weight: bold;
    margin-bottom: 10px;
}

.hero h1 {
    font-size: 48px;
    line-height: 1.2;
    margin-bottom: 20px;
    color: #263f5b;
}

.hero-text > p:not(.hello) {
    color: #68798b;
    margin-bottom: 30px;
    max-width: 500px;
}

.btn {
    display: inline-block;
    background-color: #8fc4ed;
    color: white;
    padding: 12px 25px;
    border-radius: 25px;
    text-decoration: none;
    transition: 0.3s;
}

.btn:hover {
    background-color: #5b9bd5;
    transform: translateY(-3px);
}


/* FOTO */

.hero-photo {
    width: 300px;
    height: 300px;

    background-color: #dceffc;
    border-radius: 50%;

    padding: 10px;

    display: flex;
    align-items: center;
    justify-content: center;

    box-shadow: 0 15px 35px rgba(91, 155, 213, 0.15);
}

.hero-photo img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    border-radius: 50%;
}


/* =========================
   GENERAL SECTION
========================= */

.section {
    max-width: 1100px;
    margin: auto;
    padding: 90px 25px;
}

.section-title {
    text-align: center;
    margin-bottom: 50px;
}

.section-title p {
    color: #70a9d9;
    font-size: 14px;
    margin-bottom: 5px;
}

.section-title h2 {
    font-size: 32px;
    color: #263f5b;
}


/* =========================
   ABOUT
========================= */

.about-content {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 50px;
}

.about-text,
.education {
    background-color: white;
    padding: 30px;
    border-radius: 15px;

    box-shadow: 0 8px 25px rgba(70, 120, 170, 0.08);
}

.about-text h3,
.education h3 {
    margin-bottom: 20px;
    color: #355777;
}

.about-text p {
    color: #68798b;
    margin-bottom: 15px;
}

.hobby {
    margin-top: 15px;
    padding: 10px 15px;
    background-color: #edf7ff;
    border-radius: 8px;
}

.education-item {
    display: flex;
    gap: 15px;
    margin-bottom: 18px;
    align-items: flex-start;
}

.education-item span {
    background-color: #dceffc;
    color: #4e89bb;
    padding: 5px 10px;
    border-radius: 7px;
    font-size: 12px;
    font-weight: bold;
    min-width: 50px;
    text-align: center;
}

.education-item p {
    color: #68798b;
}


/* =========================
   SKILLS
========================= */

.skills-section {
    background-color: #edf7ff;
    max-width: 100%;
}

.skills-section .section-title,
.skills-container {
    max-width: 1100px;
    margin-left: auto;
    margin-right: auto;
}

.skills-container {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 20px;
}

.skill-card {
    background-color: white;
    padding: 30px 20px;
    border-radius: 15px;
    text-align: center;

    transition: 0.3s;
}

.skill-card:hover {
    transform: translateY(-8px);
    box-shadow: 0 12px 25px rgba(70, 120, 170, 0.12);
}

.skill-icon {
    width: 55px;
    height: 55px;

    margin: 0 auto 15px;

    background-color: #dceffc;
    color: #568fc2;

    border-radius: 50%;

    display: flex;
    align-items: center;
    justify-content: center;

    font-size: 20px;
    font-weight: bold;
}

.skill-card h3 {
    margin-bottom: 10px;
    color: #355777;
}

.skill-card p {
    font-size: 14px;
    color: #748394;
}


/* =========================
   PROJECTS
========================= */

.projects-container {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 25px;
}

.project-card {
    background-color: white;
    padding: 30px;
    border-radius: 15px;

    box-shadow: 0 8px 25px rgba(70, 120, 170, 0.08);

    transition: 0.3s;
}

.project-card:hover {
    transform: translateY(-7px);
    box-shadow: 0 15px 30px rgba(70, 120, 170, 0.12);
}

.project-number {
    color: #82b8e2;
    font-size: 14px;
    font-weight: bold;
    margin-bottom: 15px;
}

.project-card h3 {
    color: #355777;
    font-size: 18px;
    margin-bottom: 15px;
}

.project-card p {
    color: #718091;
    font-size: 14px;
    margin-bottom: 15px;
}

.tools {
    display: flex;
    flex-wrap: wrap;
    gap: 7px;
    margin: 20px 0;
}

.tools span {
    background-color: #edf7ff;
    color: #548bb9;
    padding: 5px 9px;
    border-radius: 6px;
    font-size: 11px;
}

.project-link {
    color: #5b9bd5;
    text-decoration: none;
    font-size: 14px;
    font-weight: bold;
}

.project-link:hover {
    text-decoration: underline;
}


/* =========================
   CONTACT
========================= */

.contact-section {
    text-align: center;
}

.contact-container {
    max-width: 650px;
    margin: auto;
}

.contact-container > p {
    color: #718091;
    margin-bottom: 30px;
}

.contact-list {
    display: flex;
    flex-direction: column;
    gap: 15px;
}

.contact-list a {
    display: flex;
    justify-content: space-between;
    align-items: center;

    background-color: white;
    padding: 17px 20px;

    border-radius: 10px;

    text-decoration: none;
    color: #355777;

    box-shadow: 0 5px 18px rgba(70, 120, 170, 0.07);

    transition: 0.3s;
}

.contact-list a:hover {
    transform: translateX(5px);
    background-color: #edf7ff;
}

.contact-list span {
    color: #6d91b0;
}


/* =========================
   FOOTER
========================= */

footer {
    background-color: #e3f2fd;
    text-align: center;
    padding: 25px;
    color: #66829d;
    font-size: 13px;
}


/* =========================
   ANIMATION
========================= */

.fade-in {
    animation: fadeIn 1s ease;
}

@keyframes fadeIn {
    from {
        opacity: 0;
        transform: translateY(20px);
    }

    to {
        opacity: 1;
        transform: translateY(0);
    }
}


/* =========================
   RESPONSIVE TABLET
========================= */

@media (max-width: 850px) {

    .hero {
        flex-direction: column-reverse;
        text-align: center;
        padding-top: 60px;
    }

    .hero-text {
        max-width: 650px;
    }

    .hero h1 {
        font-size: 38px;
    }

    .about-content {
        grid-template-columns: 1fr;
    }

    .skills-container {
        grid-template-columns: repeat(2, 1fr);
    }

    .projects-container {
        grid-template-columns: 1fr;
    }
}


/* =========================
   RESPONSIVE HP
========================= */

@media (max-width: 600px) {

    .navbar {
        flex-direction: column;
        gap: 12px;
    }

    .nav-links {
        gap: 12px;
        flex-wrap: wrap;
        justify-content: center;
    }

    .nav-links a {
        font-size: 12px;
    }

    .hero {
        min-height: auto;
        padding: 60px 20px;
    }

    .hero-photo {
        width: 220px;
        height: 220px;
    }

    .hero h1 {
        font-size: 32px;
    }

    .section {
        padding: 65px 20px;
    }

    .section-title h2 {
        font-size: 27px;
    }

    .skills-container {
        grid-template-columns: 1fr;
    }

    .contact-list a {
        flex-direction: column;
        gap: 5px;
    }
}